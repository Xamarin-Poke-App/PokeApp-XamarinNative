<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/border_button</item>
        <item name="android:textColor">@color/button_text_color</item>
        <item name="android:textSize">16sp</item>
        <item name="android:padding">8dp</item>
    </style>

    <style name="LoginTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="OvalEditTextStyleWithBorder" parent="@android:style/Widget.EditText">
        <item name="android:background">@drawable/edittext_style</item>
        <item name="android:padding">8dp</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColorHint">#c3c3c3</item>
        <item name="android:textColor">#000000</item>
    </style>

     <!-- Phone. -->
      <style name="DefaultMargin">
        <item name="android:layout_marginTop">10dp</item>
      </style>

      <style name="DefaultImageMarginTop">
        <item name="android:layout_marginTop">30dp</item>
      </style>

      <style name="DefaultImageMarginBottom">
        <item name="android:layout_marginBottom">10dp</item>
      </style>

    <style name="pokemonDetailBackground">        
        <item name="android:background">@drawable/pokeball</item>
        <item name="android:backgroundColor">@color/bug</item>
    </style>
</resources>

