using System;
using Android.Graphics;
using Android.Util;
using Android.Content;

namespace PokeAppAndroid.View
{
    public class CurvedTopView 
    {
        /*public CurvedTopView(Context context) : base(context)
        {
            Initialize();
        }

        public CurvedTopView(Context context, IAttributeSet attrs) : base(context, attrs)
        {
            Initialize();
        }

        public CurvedTopView(Context context, IAttributeSet attrs, int defStyle) : base(context, attrs, defStyle)
        {
            Initialize();
        }

        private Path _path;

        private void Initialize()
        {
            _path = new Path();
        }

        protected override void OnDraw(Canvas canvas)
        {
            base.OnDraw(canvas);

            _path.Reset();
            _path.AddOval(new RectF(0, 0, Width, Height * 2), Path.Direction.Cw);

            using (var paint = new Paint())
            {
                paint.AntiAlias = true;
                paint.SetStyle(Paint.Style.Fill);
                paint.Color = Color.White;

                canvas.DrawPath(_path, paint);
            }
        }*/
    }
}

